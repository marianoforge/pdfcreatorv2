import { StyleSheet } from "@react-pdf/renderer";

export const additionalStyles = StyleSheet.create({
  contentContainer: {
    marginTop: 40,
  },
  title: {
    fontSize: 20,
    textAlign: "center",
    marginBottom: 20,
  },
  text: {
    fontSize: 12,
    textAlign: "center",
  },
});
